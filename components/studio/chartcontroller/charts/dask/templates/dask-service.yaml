apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dask-svc
  labels:
    run: {{ .Release.Name }}-dask-proxy-svc
spec:
  ports:
  - port: {{ .Values.scheduler.port }}
    targetPort: {{ .Values.scheduler.port }}
    protocol: TCP
  selector:
    app: {{ .Release.Name }}-dask-scheduler